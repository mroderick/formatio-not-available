{
  "private": true,
  "name": "formatio-not-available",
  "version": "1.0.0",
  "description": "A repository to investigate why people can't install formatio",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sinon": "5.0.10"
  }
}
